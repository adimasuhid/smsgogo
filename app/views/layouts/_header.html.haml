.container-narrow
  .masthead
    %ul.nav.nav-pills.pull-right
      %li{class: @active == "root" ? "active" : nil }
        =link_to "Home", root_path
      %li{class: @active == "about" ? "active" : nil }
        =link_to "About",about_path
      %li{class: @active == "pricing" ? "active" : nil }
        =link_to "Pricing", pricing_path
      %li{class: @active == "contact" ? "active" : nil }
        =link_to "Contact", contact_us_path
    .logo-header
      =link_to image_tag("logoheader-horizontal-small.png", style: "height:60px"), root_path
